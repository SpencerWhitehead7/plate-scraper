{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "experimentalDecorators": true,                      /* Enable experimental support for TC39 stage 2 draft decorators. */ // typeorm requires this
    "emitDecoratorMetadata": true,                       /* Emit design-type metadata for decorated declarations in source files. */ // typeorm requires this
    "module": "commonjs",                                /* Specify what module code is generated. */
    "typeRoots": [
      "src/@types/",
      "node_modules/@types/",
    ],                                                   /* Specify multiple folders that act like './node_modules/@types'. */
    "outDir": "./dist",                                  /* Specify an output folder for all emitted files. */
    "strictPropertyInitialization": false,               /* Check for class properties that are declared but not set in the constructor. */ // typeORM prohibits this
  }
}
